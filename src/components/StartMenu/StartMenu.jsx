import React from 'react';
import styles from './StartMenu.module.scss';

const StartMenu = () => (
  <div className={
    `startMenu 
      ${styles.startMenu}`} >
    StartMenu
  </div >
);

export default StartMenu;
